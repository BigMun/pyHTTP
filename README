Enter URL into code where "Enter URL here" is present
Script will search into the website to check if HTTP methods are possible

Wont work if website uses cloudflare

Do not use on any websites that you do not own or do not have permission to use it on.
